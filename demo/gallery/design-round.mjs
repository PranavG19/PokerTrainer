// Placeholder marker file: the design-iteration WORKFLOW script is authored inline via the Workflow
// tool (see the premium-design-directions run). This directory (demo/gallery/) is where each round's
// surviving HTML candidates are written, newest round in its own rN/ subfolder, with index.html at the
// top listing every surviving candidate across rounds. Kept designs are never deleted unless pruned for
// being objectively bad or too similar to a better sibling. The editorial direction is the anchor.
